import React from "react";
import { render } from "@testing-library/react";
import Form from "./CheckoutForm";
import TestRenderer from 'react-test-renderer';

// Write up the two tests here and make sure they are testing what the title shows

test("form header renders", async () => {};
//     //arrange
//     const {findByText} = render(<header/>);
//     //act
//     const header = findByText(/checkout/i);
//     // assert
//     expect(header).toBeInTheDocument();
// });

test("form shows success message on submit with form details", async () => {};
//     //arrange
//     const {findByText} = render(<submitted/>);
//     //act
//     const submitted = findByText(/woo-hoo/i);
//     //assert
//     expect(submitted).toBeInTheDocument();
// });
